spring:
  datasource:
    username: root
    password: root
    url: jdbc:h2:mem:db
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
#logging:
#  level:
#    org.aleksmurmur.*: DEBUG
